# Mindly-Memory-Agent
MVP of a conversational agent with persistent long-term memory. The agent remembers facts across sessions, supports multiple personas enforces per-user tenant isolation, and allows targeted forgetting.
